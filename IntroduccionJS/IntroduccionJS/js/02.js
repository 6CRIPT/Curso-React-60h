const cliente = "Juan" //constante, no puede cambiarse ni declararse. solo definirse y no cambiarlo
console.log(cliente)

let precio; // valor undefined ahora mismo. se puede cambiar, no es constante.

precio = 1000;
console.log(1000)